spring:
  application:
    name: mousecall

  datasource:
    url: jdbc:mysql://localhost:3306/mousecall?serverTimezone=Asia/Seoul&useSSL=false
    username: root
    password: 1q2w3e4r!!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # ?? ??? update, ?? ??? validate or none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

  kafka:
    bootstrap-servers: localhost:9092

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

server:
  port: 8080
